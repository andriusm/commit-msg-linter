# Commit message linter

Requires M-series chip machine to run and Apple Intelligence to be enabled in system preferences.

There's a compiled binary file attached in the release section, but it will fail to run by default. You can remove the attributes preventing that by doing `xattr -c cml` or just build it yourself in XCode.
